package main

import (
	"{{.PackagePath}}/application"
	"{{.PackagePath}}/application/infrastructure"
	"{{.PackagePath}}/application/registry"
	"{{.PackagePath}}/application/router"
)

func main() {

	myInfrastructure := infrastructure.NewMyInfrastructure()
	myRouter := router.NewMyRouter(myInfrastructure)
	myRegistry := registry.New{{PascalCase .RegistryName}}Registry(myInfrastructure)

	application.Run(myRouter, myRegistry)
}